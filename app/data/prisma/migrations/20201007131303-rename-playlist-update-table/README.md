# Migration `20201007131303-rename-playlist-update-table`

This migration has been generated by IanGrubb at 10/7/2020, 9:13:03 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."PlaylistUpdate" DROP CONSTRAINT "PlaylistUpdate_userId_fkey"

ALTER TABLE "public"."TrackAddition" DROP CONSTRAINT "TrackAddition_playlistUpdateId_fkey"

ALTER TABLE "public"."TrackRemoval" DROP CONSTRAINT "TrackRemoval_playlistUpdateId_fkey"

ALTER TABLE "public"."TrackAddition" DROP COLUMN "playlistUpdateId",
ADD COLUMN "jamsUpdateId" integer   NOT NULL 

ALTER TABLE "public"."TrackRemoval" DROP COLUMN "playlistUpdateId",
ADD COLUMN "jamsUpdateId" integer   NOT NULL 

CREATE TABLE "public"."JamsUpdate" (
"id" SERIAL,
"userId" integer   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

ALTER TABLE "public"."JamsUpdate" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."TrackAddition" ADD FOREIGN KEY ("jamsUpdateId")REFERENCES "public"."JamsUpdate"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."TrackRemoval" ADD FOREIGN KEY ("jamsUpdateId")REFERENCES "public"."JamsUpdate"("id") ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE "public"."PlaylistUpdate"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201007130903-updates-playlist-to-many-one..20201007131303-rename-playlist-update-table
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id              Int              @id @default(autoincrement())
@@ -14,10 +14,10 @@
   createdAt       DateTime         @default(now())
   followsReceived Follow[]         @relation("ReceivedBy")
   followsMade     Follow[]         @relation("MadeBy")
   playRecords     PlayRecord[]
-  Playlist        Playlist[]
-  playlistUpdates PlaylistUpdate[]
+  playlist        Playlist[]
+  jamsUpdates JamsUpdate[]
 }
 model Follow {
   id         Int      @id @default(autoincrement())
@@ -44,9 +44,9 @@
   trackAdditions TrackAddition[]
   trackRemovals  TrackRemoval[]
 }
-model PlaylistUpdate {
+model JamsUpdate {
   id             Int             @id @default(autoincrement())
   userId         Int
   createdAt      DateTime        @default(now())
   user           User            @relation(fields: [userId], references: [id])
@@ -56,26 +56,25 @@
 model TrackAddition {
   id               Int            @id @default(autoincrement())
   trackId          Int
-  playlistUpdateId Int
-  playlistUpdate   PlaylistUpdate @relation(fields: [playlistUpdateId], references: [id])
+  jamsUpdateId Int
+  jamsUpdate   JamsUpdate @relation(fields: [jamsUpdateId], references: [id])
   track            Track          @relation(fields: [trackId], references: [id])
 }
 model TrackRemoval {
   id               Int            @id @default(autoincrement())
   trackId          Int
-  playlistUpdateId Int
-  playlistUpdate   PlaylistUpdate @relation(fields: [playlistUpdateId], references: [id])
+  jamsUpdateId Int
+  jamsUpdate   JamsUpdate @relation(fields: [jamsUpdateId], references: [id])
   track            Track          @relation(fields: [trackId], references: [id])
 }
 model Playlist {
-  id            Int             @id @default(autoincrement())
-  spotify_id    String
-  name          String
-  userId        Int
-  user          User            @relation(fields: [userId], references: [id])
-  createdAt      DateTime        @default(now())
+  id         Int      @id @default(autoincrement())
+  name       String
+  userId     Int
+  spotify_id String
+  createdAt  DateTime @default(now())
+  user       User     @relation(fields: [userId], references: [id])
 }
-
```


